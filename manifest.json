{
  "name": "pixiv",
  "version": "1.4.0",
  "apiVersion": 1,
  "permissions": [
    "net"
  ],
  "depends": {},
  "conflicts": [],
  "commands": [
    {
      "name": "pixiv",
      "description": "Pixiv 随机美图 / 作品详情",
      "usage": "请按格式使用：\n/pixiv random [尺寸]\n/pixiv illust [作品id]"
    }
  ],
  "regex": [],
  "events": [],
  "buttons": [],
  "cron": [],
  "routes": [
    {
      "method": "GET",
      "path": "/img",
      "auth": "public"
    }
  ],
  "hasMiddleware": false,
  "services": [],
  "durableObjects": [],
  "displayName": "Pixiv 图床",
  "description": "随机获取 Pixiv 美图、按作品 ID 查询详情（pixiv.yuki.sh 第三方图床）",
  "configSchema": {
    "type": "object",
    "properties": {
      "show_image_info": {
        "type": "boolean",
        "title": "随机图片是否显示作品信息（标题、作者、标签）",
        "default": true
      },
      "default_image_size": {
        "type": "string",
        "title": "随机图片默认尺寸",
        "enum": [
          "mini",
          "thumb",
          "small",
          "regular",
          "original"
        ],
        "default": "regular",
        "description": "regular 为 master1200 jpg；Free 套餐单请求 CPU 上限 10 ms，超出预算的图会自动回落小一档"
      },
      "max_image_mb": {
        "type": "number",
        "title": "单张图片体积上限（MB，仅 base64 路径生效）",
        "default": 20,
        "minimum": 0.5,
        "maximum": 20,
        "description": "超过此体积的图会回落小一档。默认等于上限（20 MB，即不做人为限制）。仅 base64 路径生效：配了 public_base_url 走 URL 直传后图片不经插件下载，本项被忽略。注意 Free 套餐单请求 CPU 上限 10 ms，base64 路径的安全区只有约 3 MB——在 Free 上且 URL 直传不可用时建议调到 2，否则超过约 3 MB 的图会撞 Error 1102 整条失败，而不是回落小一档。Paid 套餐（默认 30 s CPU）无需调整"
      },
      "public_base_url": {
        "type": "string",
        "title": "Worker 公开地址（走 URL 直传，可绕开 CPU 上限）",
        "default": "",
        "description": "填 Worker 的公开访问地址（如 https://bot.example.com），图片改由平台自己来拉，插件侧 CPU 归零、不受体积上限约束。留空则回退 base64 直传。只接受 https。填了之后建议实测一张：若平台拉不到该域名（workers.dev 在国内可达性不稳），本条会自动回退 base64 并在日志留下 warn，不会静默丢图"
      },
      "show_buttons": {
        "type": "boolean",
        "title": "在文本消息上挂内嵌按钮",
        "default": true,
        "description": "在作品信息与帮助文案下方挂\"再来一张\"等按钮，点击即自动发送对应指令。按钮需要 bot 侧开通（官方文档把自定义按钮标为\"内邀开通\"）；未开通时发送会失败，插件会自动去掉按钮重试一次，最坏退化成纯文本，不会丢消息。注意图片消息挂不了按钮——按钮只能挂在 markdown 文本消息上"
      },
      "message_order": {
        "type": "string",
        "title": "文本与图片的发送顺序",
        "default": "text_first",
        "enum": [
          "text_first",
          "image_first"
        ],
        "description": "text_first：先发作品信息（按钮紧贴其下），再发图片；image_first：先发图片，作品信息连同按钮落在最底部。想按按钮时少滚一点选 image_first；图片发送失败时该模式不会发文案（描述一张没发出的图没有意义）"
      }
    }
  },
  "defaultConfig": {
    "show_image_info": true,
    "default_image_size": "regular",
    "max_image_mb": 20,
    "public_base_url": "",
    "show_buttons": true,
    "message_order": "text_first"
  }
}
