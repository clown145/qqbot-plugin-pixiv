{
  "name": "pixiv",
  "version": "1.3.1",
  "apiVersion": 1,
  "permissions": [
    "net"
  ],
  "depends": {},
  "conflicts": [],
  "commands": [
    {
      "name": "pixiv",
      "description": "Pixiv 随机美图 / 作品详情",
      "usage": "请按格式使用：\n/pixiv random [尺寸]\n/pixiv illust [作品id]"
    }
  ],
  "regex": [],
  "events": [],
  "buttons": [],
  "cron": [],
  "routes": [
    {
      "method": "GET",
      "path": "/img",
      "auth": "public"
    }
  ],
  "hasMiddleware": false,
  "services": [],
  "durableObjects": [],
  "displayName": "Pixiv 图床",
  "description": "随机获取 Pixiv 美图、按作品 ID 查询详情（pixiv.yuki.sh 第三方图床）",
  "configSchema": {
    "type": "object",
    "properties": {
      "show_image_info": {
        "type": "boolean",
        "title": "随机图片是否显示作品信息（标题、作者、标签）",
        "default": true
      },
      "default_image_size": {
        "type": "string",
        "title": "随机图片默认尺寸",
        "enum": [
          "mini",
          "thumb",
          "small",
          "regular",
          "original"
        ],
        "default": "regular",
        "description": "regular 为 master1200 jpg；Free 套餐单请求 CPU 上限 10 ms，超出预算的图会自动回落小一档"
      },
      "max_image_mb": {
        "type": "number",
        "title": "单张图片体积上限（MB，仅 base64 路径生效）",
        "default": 20,
        "minimum": 0.5,
        "maximum": 20,
        "description": "超过此体积的图会回落小一档。默认等于上限（20 MB，即不做人为限制）。仅 base64 路径生效：配了 public_base_url 走 URL 直传后图片不经插件下载，本项被忽略。注意 Free 套餐单请求 CPU 上限 10 ms，base64 路径的安全区只有约 3 MB——在 Free 上且 URL 直传不可用时建议调到 2，否则超过约 3 MB 的图会撞 Error 1102 整条失败，而不是回落小一档。Paid 套餐（默认 30 s CPU）无需调整"
      },
      "public_base_url": {
        "type": "string",
        "title": "Worker 公开地址（走 URL 直传，可绕开 CPU 上限）",
        "default": "",
        "description": "填 Worker 的公开访问地址（如 https://bot.example.com），图片改由 QQ 自己来拉，插件侧 CPU 归零、无体积上限。留空则回退 base64 直传。注意：QQ 是后台异步拉取，拉不到时不会报错、只会静默不出图，设置后务必实测一张能否收到；收不到就清空此项"
      }
    }
  },
  "defaultConfig": {
    "show_image_info": true,
    "default_image_size": "regular",
    "max_image_mb": 20,
    "public_base_url": ""
  }
}
